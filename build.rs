// build.rs — intentionally empty.
// To suppress the console window on Windows, add this line at the top of each app's main .rs file:
//   #![cfg_attr(target_os = "windows", windows_subsystem = "windows")]
fn main() {}
